{% extends 'base.html.twig' %}

{% block body %}
       <div class="container-fluid">
        <div class="container">
            <div class="table" >
                {% if menu is defined %}
                   {{ menu|raw }}
                {% endif %}
            </div>
        </div>
    </div>
 {% endblock %}

